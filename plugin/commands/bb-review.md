---
description: Bitbucket PR을 bb-mcp로 가져와 리뷰 코멘트를 작성한다 (출력 언어는 PR 기준, --ko/--en 로 지정)
argument-hint: [workspace/repo] [PR번호] [경로] [--ko|--en]
---

`bb-pr-review` 스킬을 따라 Bitbucket PR을 리뷰한다.

인자: $ARGUMENTS

- 인자가 없으면 `bb_pr_inbox`로 열린 PR을 보여주고 어느 것을 리뷰할지 묻는다.
- `<workspace>/<repo> <번호>` 형태면 그 PR을 바로 리뷰한다.
- 세 번째 인자로 경로를 주면 그 파일만 본다.
- `--ko` / `--en` 은 **출력 언어**다. 없으면 PR 의 언어를 따른다 — 스킬의
  「출력 언어」 절 참고. 위치는 상관없고, 인자에서 빼고 나머지를 해석한다.

스킬의 실행 순서(대상 확정 → 파일 단위 diff → 기존 코멘트 확인 → 규약 로드 →
배포 경로 → 라인 검증 → base 대조 → 재현 완주 → 도달 가능성)를 건너뛰지 않는다.
특히 `bb_pr_diff`는 `path` 없이 호출하지 않는다.

출력은 마크다운 본문만. **게시는 사용자가 명시적으로 요청할 때만** 한다.

툴이 안 붙거나 401/403이 나면 먼저 `bb_doctor`를 부른다.

저장소·번호를 안 받았으면 `bb_detect_repo()` 로 현재 폴더를 먼저 확인한다.
감지되면 그 저장소의 열린 PR 중에서 고르게 한다.

저장소를 정하지 못하면 `AskUserQuestion` 선택창으로 고르게 한다 —
평문으로 되묻지 않는다. 열린 PR이 있는 저장소를 앞에 둔다.

PR 목록을 보여줄 때는 스킬의 **"PR 목록을 보여줄 때"** 규약을 따른다 —
마크다운 표, 열 순서 고정, 승인 상태는 목록에 넣지 않는다.

리뷰 본문의 포맷은 별개다. 스킬의 `## 출력` 절이 정한 고정 포맷을 그대로 쓴다
(6개 분류 이모지, `path:LINE`, `### 🔴 1-1.`, `## 종합 의견`). 목록 표 규약을
리뷰 본문에 적용하지 않는다 — 지적 사항을 표로 압축하면 ①무엇 ②재현 ③수정이
들어갈 자리가 없어진다.

**분량은 스킬의 「분량 — 세어서 지킨다」 절 상한을 지킨다** (🔴 12줄 · 나머지 5줄 ·
종합 의견 2줄). 넘치면 지적을 빼지 말고 문장을 뺀다.

이전 리뷰가 있고 커밋이 그 뒤로 움직였으면 스킬의 **「재리뷰」 절**을 따른다 —
머리말·「이전 리뷰 처리」 표, 이미 게시한 지적은 번호로만 참조.
