node_modules/

# 허용 저장소 목록. 사내 저장소 이름이 들어간다.
.config/

# 토큰이 평문으로 들어갈 수 있는 파일
.env
*.local.json

# 루트 .mcp.json 은 두지 않는다. 이 저장소가 그 자체로 Claude Code
# 프로젝트라서, 프로젝트 스코프 MCP 설정으로 읽혀 user 스코프 등록을
# 덮어쓴다. 배포용 선언은 plugin-template/mcp.json 에 있다.
.mcp.json

# Claude Code 가 프로젝트에 만드는 로컬 설정
.claude/
