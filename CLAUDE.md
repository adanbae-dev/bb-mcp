# bb-mcp — 작업 규칙

Bitbucket Cloud PR을 읽고 리뷰 코멘트를 쓰는 개인용 MCP 서버.
설계 근거는 [README.md](./README.md), 무엇을 왜 틀렸는지는 [CHANGELOG.md](./CHANGELOG.md).

아래는 **이 저장소에서 반복해서 대가를 치른 것들**이다. 일반적인 모범 사례가 아니라
여기서 실제로 깨진 것만 적는다.

## 1. 예시에 실제 이름을 쓰지 않는다

이 저장소는 공개 원격이 있다. 예시·픽스처·문서에는 플레이스홀더만 쓴다.

| 종류 | 쓸 것 |
|---|---|
| 저장소 | `acme/web-app`, `acme/admin-web`, `acme/api` |
| 사람 | 홍길동 · 김철수 · 이영희 |
| 티켓 | `PROJ-0000` |

`test/forbidden.test.mjs` 가 이것을 **강제한다**(해시 대조). 걸리면 위치만 알려준다 —
걸린 문자열을 출력하면 CI 로그가 또 하나의 유출 경로가 된다.

**현재 트리만 검사한다.** 0.18.0 이전 커밋에는 실제 이름이 남아 있고, 히스토리
재작성은 하지 않기로 결정했다(근거는 [README.md §7 잔존 위험 ⑩](./README.md)).
그래서 이 테스트는 **앞으로만** 막는다 — 새로 넣지 않는 것이 유일한 방어다.

**한 번 정리하면 끝나는 문제가 아니다.** 새 예시를 쓸 때 손에 잡히는 데이터는 늘
방금 툴로 조회한 실제 데이터다. 두 번 새어나갔고, 두 번째는 이전 커밋에 남아 있던
것이었다. 세 번째는 이 테스트를 추가한 커밋에서 곧바로 잡혔다.

## 2. 한 번 관측한 증상을 원인으로 승격시킬 때 표시를 남긴다

리뷰 코멘트에는 `추정:` / `확인 필요:` 를 강제하는데, **내가 쓰는 기록에는 안 썼다.**
그래서 틀린 진단이 CHANGELOG·일지·대화로 유통됐다.

`bb_get` 의 `fields` 를 "목록 응답에서 배열을 순회하지 못한다" 고 적었다. 고치려고
코드를 열었을 때 원소 기준 경로는 **원래 됐다는 것**이 드러났다. 깨지는 것은
`values.` 접두사를 붙인 형태뿐이었다. 한 번 당한 증상을 원인으로 결론짓고 그 문장을
네 번 옮겨 적었다.

- 재현을 한 번만 해봤으면 `추정:` 을 붙인다
- **고치기 전에 재현부터 한다.** 그 순서가 아니면 없는 버그를 고치게 된다
- 증상 범위를 단정하기 전에 반대 경우(되는 경우)를 찾아본다

## 3. 문서 안 상호 참조는 번호가 아니라 제목으로

절 번호는 절이 하나 추가되면 조용히 밀린다. **문서 안의 참조는 틀려도 아무 데서도
안 터진다** — 코드라면 함수명이 틀리면 죽는데, "6단계" 는 그냥 잘못된 곳을 가리킨다.

게시 절이 "6단계에서 검증한 번호" 라고 가리키는 동안 라인 검증은 8단계였고, 그
결과 인라인 코멘트가 엉뚱한 줄에 달렸다. 고친 뒤에도 두 곳이 더 틀려 있었다.

## 4. 예시가 규칙을 이긴다

규칙 문장과 예시가 어긋나면 예시대로 동작한다. 규약을 고칠 때 **예시 표·코드블록을
같이 고친다.** 같은 규칙이 여러 파일에 있으면 전부 찾는다 —
표 규약은 `plugin/commands/bb-prs.md` · `plugin/skills/bb-pr-review/SKILL.md` ·
`README.md` 세 곳에 있다.

## 5. 조용한 실패를 남기지 않는다

이 저장소에서 시간을 가장 많이 먹은 것은 터지는 버그가 아니라 조용한 쪽이었다.

- `setup.sh` 6단계가 경로가 바뀐 뒤 아무 일도 하지 않고 `warn` 만 했다
- `npm test` 가 파일 3개를 하드코딩해서 **새 테스트 파일이 실행되지 않았다**
- `pick` 이 경로 오타와 '값이 비어 있음' 을 구분하지 않고 `{}` 를 돌려줬다
- allowlist 미설정이 `open`(전부 허용)으로 떴다

새 분기를 만들 때 "실패했는데 조용히 넘어가는 경로" 가 생기는지 본다.

## 6. 쓰기는 기본 차단, 승인은 사람이

게이트 5개(`ALLOW_COMMENT` · `ALLOW_WRITE` · `ALLOW_ALLOWLIST_WRITE` ·
`ALLOW_PR_CREATE` · `ALLOW_ALL_REPOS`)는 각각 따로 켠다 — 리뷰하려고 머지 권한을
열 필요가 없다.

**PR 승인·머지 툴은 일부러 만들지 않았다.** 요청받아도 만들지 않는다.
게시된 코멘트는 되돌릴 수단이 없으므로(Bitbucket API 에 pending 생성 경로가 없다)
게시 전 확인과 게시 후 되읽기가 필수다.

**코멘트 게시를 시험할 때는 팀 PR 을 쓰지 않는다.** `ALLOW_COMMENT` 는 켜져 있고
`ALLOW_WRITE` 는 꺼져 있으므로 **달 수는 있고 지울 수는 없다.** 지우려면 머지·삭제
권한까지 함께 열어야 해서, 코멘트 하나 때문에 게이트를 쪼갠 이유를 무너뜨리게 된다.
실제로 한 번 남겼고(2026-09, 이미 머지된 PR) 결국 그대로 두기로 했다.
시험은 본인이 만든 PR 에서 하고, 그게 없으면 게시 없이 출력까지만 확인한다.

## 7. 릴리스

버전이 **세 곳**에 있고 전부 맞아야 한다. 순서와 이유는
[README.md §1-1](./README.md) — 푸시가 `marketplace update` 보다 먼저다.

```bash
npm test && claude plugin validate ./plugin && claude plugin validate .
```
